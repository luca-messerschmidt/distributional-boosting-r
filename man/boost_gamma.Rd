% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/boost_gamma.R
\name{boost_gamma}
\alias{boost_gamma}
\title{Distributional Gamma Gradient Boosting}
\usage{
boost_gamma(
  X = NULL,
  Z = NULL,
  y = NULL,
  formula = NULL,
  data = NULL,
  mstop = 100,
  nu_mu = 0.1,
  nu_shape = 0.1,
  method = c("cyclic", "noncyclic"),
  patience = NULL,
  validation_split = 0.2,
  seed = NULL,
  learner = c("linear", "spline", "auto")
)
}
\arguments{
\item{X}{A data frame or matrix of predictors for the mean submodel.
Ignored if \code{formula} is supplied.}

\item{Z}{A data frame or matrix of predictors for the shape submodel.
Ignored if \code{formula} is supplied.}

\item{y}{A numeric response vector; every value must be strictly positive.
Ignored if \code{formula} is supplied.}

\item{formula}{A list with components \code{mu} (a two-sided formula) and
\code{shape} (a one-sided formula). When supplied, \code{X}, \code{Z},
and \code{y} are constructed from \code{data} instead.}

\item{data}{A data frame containing the variables referenced in
\code{formula}. Required (and only used) when \code{formula} is supplied.}

\item{mstop}{Number of boosting iterations. Either a single positive
integer or, for \code{method = "cyclic"} only, \code{c(mu = ..., shape = ...)}.}

\item{nu_mu}{Learning rate for the mean submodel.}

\item{nu_shape}{Learning rate for the shape submodel.}

\item{method}{Boosting update schedule, \code{"cyclic"} (default) or
\code{"noncyclic"} -- see \code{\link{boost_gaussian}} for details.}

\item{patience}{Optional internal early-stopping patience (see
\code{\link{boost_gaussian}}).}

\item{validation_split}{Fraction held out for early-stopping search.}

\item{seed}{Optional seed for the early-stopping train/validation split.}

\item{learner}{Base learner type: \code{"linear"} (default), \code{"spline"}
(df-equalized P-splines), or \code{"auto"} (best of both per column).}
}
\value{
A list of class \code{c("boost_gamma", "boost_dist")}.
}
\description{
Fits a component-wise gradient boosting model for strictly positive,
right-skewed responses, jointly estimating the Gamma mean (\eqn{\mu}) and
shape (\eqn{\nu}) parameters (both log-linked). Built on the same
family-driven boosting engine as \code{\link{boost_gaussian}}.
}
\examples{
set.seed(123)
X <- data.frame(x1 = rnorm(150))
Z <- data.frame(z1 = rnorm(150))
mu    <- exp(1 + 0.5 * X$x1)
shape <- exp(0.3 * Z$z1 + 1)
y <- rgamma(150, shape = shape, rate = shape / mu)

fit <- boost_gamma(X, Z, y, mstop = 100)
}
